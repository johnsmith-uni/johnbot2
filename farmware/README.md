## Firmware Dependencies

The firmware runs on the ESP32 platform and requires the following Arduino libraries:

- **WiFi.h** (built into ESP32 Arduino core)
- **ArduinoOSCWiFi**  
  https://github.com/3110/ArduinoOSC

Install via:

- Arduino IDE: *Library Manager → search "ArduinoOSC"*
- PlatformIO: add to `platformio.ini`:
